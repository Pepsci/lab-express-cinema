<h1>Movies page</h1>

<div class="container-movies">
    {{#each movies}}
        <div class="card">
            <img src="{{this.image}}" alt="{{this.title}}">
            <p>{{this.title}}</p>
            <a href="/movie/{{this._id}}">See more</a>
        </div>
    {{/each}}
</div>